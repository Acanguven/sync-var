import {SyncVar} from "../lib";
import {expect} from "chai";


describe('Library', () => {
  it('should test', () => {
    //Arrange
    expect(SyncVar).to.be.a('function');
    //Act

    //Assert

  });
});
