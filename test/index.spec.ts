import {Test} from "../lib";
import {expect} from "chai";


describe('Library', () => {
  it('should test', () => {
    //Arrange
    expect(Test).to.be.a('function');
    //Act

    //Assert

  });
});
